function handleBooking(roomId,roomNo) {
    // let xttpPost = new XMLHttpRequest();
    // xttpPost.open('POST', '../../View/Make Reservation/confirm_booking_guest.php', true);

    // xttpPost.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded');

   
    // let data = 'roomId=' + encodeURIComponent(roomId) +
    //     '&check_in=' + encodeURIComponent(check_in) +
    //     '&check_out=' + encodeURIComponent(check_out);

    // xttpPost.send(data);
    // window.location.href = '../../View/Make Reservation/confirm_booking_guest.php';
    // xttpPost.onreadystatechange = function () {
    //     if (this.readyState === 4 && this.status === 200) {
    //         alert('Booking Successful');
    //     }
        
    // }
}
